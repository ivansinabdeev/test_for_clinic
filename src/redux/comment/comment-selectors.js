// export const getComments = (state) => state.comment.comments;
// export const getFilter = (state) => state.comment.filter;

export const getContacts = (state) => state.comment.contacts;
export const getFilter = (state) => state.comment.filter;
